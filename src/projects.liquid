---
title: Projects
eleventyExcludeFromCollections: true
---
<h2>This is where our liquid templating goes for showing projects</h2>

<ul>
  {% for project in projects %}
    <li class="bg-white p-4 my-4 rounded hover:shadow hover:scale-105">
      <a class="text-black hover:text-black" href="{{project.link}}" target="_blank">
        <div class="flex flex-row gap-2 prose items-center">
          <img
            src="{{project.logo}}"
            class="w-8 h-8 m-0"
          >
          <h3 class="m-0">{{ project.name }}</h3>
          <div class="grow flex justify-end gap-2 items-center text-sm">
            <span
              class="
                whitespace-nowrap px-2 rounded bg-zinc-200
                {% if project.status == "Archived" %}opacity-45
                {% endif %}
              "
            >
              {{- project.turnover -}}
            </span>
            <span
              class="
                whitespace-nowrap px-2 rounded
                {% if project.status == "Active" %}bg-green-200
                {% elsif project.status == "Building" %}bg-sky-200
                {% else %}bg-zinc-300
                {% endif %}
              "
            >
              {{- project.status -}}
            </span>
          </div>
        </div>
        <p class="text-base-content/80 text-sm lg:text-base">{{ project.description }}</p>
      </a>
    </li>
  {% endfor %}
</ul>
